# Training data

Create `ai_text_dataset.csv` with two columns: `text` and `label`. Labels must be exactly `human` or `ai`.

Do not train with the two placeholder rows in the example file. Supply at least 40 valid balanced rows for the script to run; hundreds or thousands of diverse, accurately labelled samples are recommended. Keep author/topic overlap across train and test data under control to avoid misleading evaluation results.
